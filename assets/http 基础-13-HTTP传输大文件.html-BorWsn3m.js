import{_ as e,c as t,o as n,a as o}from"./app-BR1jJyyr.js";const d="/images/s_poetries_work_gitee_2019_12_18.webp",c="/images/s_poetries_work_gitee_2019_12_19.webp",r="/images/s_poetries_work_gitee_2019_12_101.webp",i={},a=o('<h2 id="简版速记" tabindex="-1"><a class="header-anchor" href="#简版速记"><span>简版速记</span></a></h2><table><thead><tr><th>手段</th><th>关键请求头 / 响应头</th><th>状态码</th><th>典型场景</th></tr></thead><tbody><tr><td>数据压缩</td><td><code>Accept-Encoding</code> / <code>Content-Encoding: gzip|br</code></td><td>200</td><td>HTML/CSS/JS 等文本</td></tr><tr><td>分块传输</td><td><code>Transfer-Encoding: chunked</code></td><td>200</td><td>流式/动态内容，body 长度未知</td></tr><tr><td>范围请求（单段）</td><td><code>Range: bytes=x-y</code> / <code>Content-Range: bytes x-y/total</code></td><td>206</td><td>视频拖拽、断点续传</td></tr><tr><td>范围请求（多段）</td><td><code>Range: bytes=x1-y1,x2-y2</code> / <code>Content-Type: multipart/byteranges; boundary=xxx</code></td><td>206</td><td>一次请求多个不连续片段</td></tr></tbody></table><p><strong>记忆要点：</strong></p><ul><li><code>Transfer-Encoding: chunked</code> 与 <code>Content-Length</code> <strong>互斥</strong>，二者不可同时出现</li><li>范围越界 → <strong>416 Range Not Satisfiable</strong>；合法范围 → <strong>206 Partial Content</strong></li><li>分块格式：<code>十六进制长度\\r\\n数据块\\r\\n</code>，以 <code>0\\r\\n\\r\\n</code> 终止</li><li>范围请求服务器需先声明 <code>Accept-Ranges: bytes</code>；不支持则发 <code>Accept-Ranges: none</code> 或省略该字段</li><li>四种方法<strong>可叠加</strong>：压缩 + 分块、范围请求 + 分块均合法</li></ul><h2 id="数据压缩" tabindex="-1"><a class="header-anchor" href="#数据压缩"><span>数据压缩</span></a></h2><p>通常浏览器在发送请求时都会带着“Accept-Encoding”头字段，里面是浏览器支持的压缩格式列表，例如 gzip、deflate、br 等，这样服务器就可以从中选择一种压缩算法，放进“Content-Encoding”响应头里，再把原数据压缩后发给浏览器。</p><p>如果压缩率能有 50%，也就是说 100K 的数据能够压缩成 50K 的大小，那么就相当于在带宽不变的情况下网速提升了一倍，加速的效果是非常明显的。</p><p>不过这个解决方法也有个缺点，gzip 等压缩算法通常只对文本文件有较好的压缩率，而图片、音频视频等多媒体数据本身就已经是高度压缩的，再用 gzip 处理也不会变小（甚至还有可能会增大一点），所以它就失效了。</p><p>不过数据压缩在处理文本的时候效果还是很好的，所以各大网站的服务器都会使用这个手段作为”保底”。例如，在 Nginx 里就会使用”gzip on”指令，启用对”text/html”的压缩</p><blockquote><p>补充（现代做法）：<strong>Brotli（<code>br</code>）已成为主流首选压缩算法</strong>。与 gzip 相比，Brotli 对 HTML/CSS/JS 的压缩率通常高 15–25%，且已被所有现代浏览器支持。Nginx 需安装 <code>ngx_brotli</code> 模块并配置 <code>brotli on; brotli_comp_level 6;</code>；Cloudflare、Caddy 等 CDN/服务器已默认开启。实践中优先级：<code>br</code> &gt; <code>zstd</code>（新兴）&gt; <code>gzip</code>（兜底）。</p></blockquote><h2 id="分块传输" tabindex="-1"><a class="header-anchor" href="#分块传输"><span>分块传输</span></a></h2><p>在数据压缩之外，还能有什么办法来解决大文件的问题呢？</p><p>压缩是把大文件整体变小，我们可以反过来思考，如果大文件整体不能变小，那就把它“拆开”，分解成多个小块，把这些小块分批发给浏览器，浏览器收到后再组装复原。</p><p>这样浏览器和服务器都不用在内存里保存文件的全部，每次只收发一小部分，网络也不会被大文件长时间占用，内存、带宽等资源也就节省下来了。</p><p>这种“化整为零”的思路在 HTTP 协议里就是“chunked”分块传输编码，在响应报文里用头字段“Transfer-Encoding: chunked”来表示，意思是报文里的 body 部分不是一次性发过来的，而是分成了许多的块（chunk）逐个发送。</p><p>这就好比是用魔法把大象变成“乐高积木”，拆散了逐个装进冰箱，到达目的地后再施法拼起来“满血复活”。</p><p>分块传输也可以用于“流式数据”，例如由数据库动态生成的表单页面，这种情况下 body 数据的长度是未知的，无法在头字段“Content- Length”里给出确切的长度，所以也只能用 chunked 方式分块发送。</p><p>“Transfer-Encoding: chunked”和“Content- Length”这两个字段是互斥的，也就是说响应报文里这两个字段不能同时出现，一个响应报文的传输要么是长度已知，要么是长度未知（chunked），这一点你一定要记住。</p><blockquote><p>下面我们来看一下分块传输的编码规则，其实也很简单，同样采用了明文的方式，很类似响应头</p></blockquote><ul><li>每个分块包含两个部分，长度头和数据块；</li><li>长度头是以 CRLF（回车换行，即<code>\\r\\n</code>）结尾的一行明文，用 16 进制数字表示长度；</li><li>数据块紧跟在长度头后，最后也用 <code>CRLF</code> 结尾，但数据不包含 CRLF；</li><li>最后用一个长度为 0 的块表示结束，即“<code>0\\r\\n\\r\\n</code>”</li></ul><p><img src="'+d+'" alt=""></p><blockquote><p>补充（现代做法）：<strong>HTTP/2 废除了 <code>Transfer-Encoding: chunked</code></strong>。HTTP/2 自带二进制分帧（DATA frame）机制，天然支持流式传输，chunked 编码在 HTTP/2 及以上版本中无效；服务端推流（Server-Sent Events、gRPC streaming）均依赖 HTTP/2 帧而非 chunked。如果你的服务运行在 HTTP/2，无需也不应再设置 <code>Transfer-Encoding: chunked</code>，该字段只在 HTTP/1.1 连接上生效。</p></blockquote><h2 id="范围请求" tabindex="-1"><a class="header-anchor" href="#范围请求"><span>范围请求</span></a></h2><p>有了分块传输编码，服务器就可以轻松地收发大文件了，但对于上 G 的超大文件，还有一些问题需要考虑。</p><p>比如，你在看当下正热播的某穿越剧，想跳过片头，直接看正片，或者有段剧情很无聊，想拖动进度条快进几分钟，这实际上是想获取一个大文件其中的片段数据，而分块传输并没有这个能力。</p><p>HTTP 协议为了满足这样的需求，提出了“范围请求”（range requests）的概念，允许客户端在请求头里使用专用字段来表示只获取文件的一部分，相当于是客户端的“化整为零”。</p><p>范围请求不是 Web 服务器必备的功能，可以实现也可以不实现，所以服务器必须在响应头里使用字段“Accept-Ranges: bytes”明确告知客户端：“我是支持范围请求的”。</p><p>如果不支持的话该怎么办呢？服务器可以发送“Accept-Ranges: none”，或者干脆不发送“Accept- Ranges”字段，这样客户端就认为服务器没有实现范围请求功能，只能老老实实地收发整块文件了。</p><p>请求头Range是 HTTP 范围请求的专用字段，格式是“bytes=x-y”，其中的 x 和 y 是以字节为单位的数据范围。</p><p>要注意 x、y 表示的是“偏移量”，范围必须从 0 计数，例如前 10 个字节表示为“0-9”，第二个 10 字节表示为“10-19”，而“0-10”实际上是前 11 个字节。</p><p>Range 的格式也很灵活，起点 x 和终点 y 可以省略，能够很方便地表示正数或者倒数的范围。假设文件是 100 个字节，那么</p><ul><li>“0-”表示从文档起点到文档终点，相当于“0-99”，即整个文件；</li><li>“10-”是从第 10 个字节开始到文档末尾，相当于“10-99”；</li><li>“-1”是文档的最后一个字节，相当于“99-99”；</li><li>“-10”是从文档末尾倒数 10 个字节，相当于“90-99”</li></ul><p><strong>服务器收到 Range 字段后，需要做四件事。</strong></p><p>第一，它必须检查范围是否合法，比如文件只有 100 个字节，但请求“200-300”，这就是范围越界了。服务器就会返回状态码416，意思是“你的范围请求有误，我无法处理，请再检查一下”。</p><p>第二，如果范围正确，服务器就可以根据 Range 头计算偏移量，读取文件的片段了，返回状态码“206 Partial Content”，和 200 的意思差不多，但表示 body 只是原数据的一部分。</p><p>第三，服务器要添加一个响应头字段Content-Range，告诉片段的实际偏移量和资源的总大小，格式是“bytes x-y/length”，与 Range 头区别在没有“=”，范围后多了总长度。例如，对于“0-10”的范围请求，值就是“bytes 0-10/100”。</p><p>最后剩下的就是发送数据了，直接把片段用TCP发给客户端，一个范围请求就算是处理完了</p><h2 id="多段数据" tabindex="-1"><a class="header-anchor" href="#多段数据"><span>多段数据</span></a></h2><p>刚才说的范围请求一次只获取一个片段，其实它还支持在 Range 头里使用多个“x-y”，一次性获取多个片段数据。</p><p>这种情况需要使用一种特殊的 MIME 类型：“multipart/byteranges”，表示报文的 body 是由多段字节序列组成的，并且还要用一个参数“boundary=xxx”给出段之间的分隔标记。</p><p>多段数据的格式与分块传输也比较类似，但它需要用分隔标记 boundary 来区分不同的片段，可以通过图来对比一下</p><p><img src="'+c+'" alt=""></p><p>每一个分段必须以“- -boundary”开始（前面加两个“-”），之后要用“Content-Type”和“Content- Range”标记这段数据的类型和所在范围，然后就像普通的响应头一样以回车换行结束，再加上分段数据，最后用一个“- -boundary- -”（前后各有两个“-”）表示所有的分段结束</p><h2 id="小结" tabindex="-1"><a class="header-anchor" href="#小结"><span>小结</span></a></h2><ul><li>压缩 HTML 等文本文件是传输大文件最基本的方法；</li><li>分块传输可以流式收发数据，节约内存和带宽，使用响应头字段“<code>Transfer-Encoding: chunked</code>”来表示，分块的格式是 16 进制长度头 + 数据块；</li><li>范围请求可以只获取部分数据，即“分块请求”，实现视频拖拽或者断点续传，使用请求头字段“<code>Range</code>”和响应头字段“<code>Content-Range</code>”，响应状态码必须是 206；</li><li>也可以一次请求多个范围，这时候响应报文的数据类型是“<code>multipart/byteranges</code>”，<code>body</code> 里的多个部分会用 <code>boundary</code> 字符串分隔</li></ul><blockquote><p>要注意这四种方法不是互斥的，而是可以混合起来使用，例如压缩后再分块传输，或者分段后再分块</p></blockquote><p><img src="'+r+'" alt=""></p><p>阅读全文</p>',48),p=[a];function l(s,g){return n(),t("div",null,p)}const _=e(i,[["render",l],["__file","http 基础-13-HTTP传输大文件.html.vue"]]),u=JSON.parse('{"path":"/http%20%E5%9F%BA%E7%A1%80-13-HTTP%E4%BC%A0%E8%BE%93%E5%A4%A7%E6%96%87%E4%BB%B6.html","title":"","lang":"zh-CN","frontmatter":{"description":"简版速记 记忆要点： Transfer-Encoding: chunked 与 Content-Length 互斥，二者不可同时出现 范围越界 → 416 Range Not Satisfiable；合法范围 → 206 Partial Content 分块格式：十六进制长度\\\\r\\\\n数据块\\\\r\\\\n，以 0\\\\r\\\\n\\\\r\\\\n 终止 范围请求服务器需先声明 ...","head":[["meta",{"property":"og:url","content":"https://interview.leeguoo.com/http%20%E5%9F%BA%E7%A1%80-13-HTTP%E4%BC%A0%E8%BE%93%E5%A4%A7%E6%96%87%E4%BB%B6.html"}],["meta",{"property":"og:site_name","content":"前端面试题集锦"}],["meta",{"property":"og:description","content":"简版速记 记忆要点： Transfer-Encoding: chunked 与 Content-Length 互斥，二者不可同时出现 范围越界 → 416 Range Not Satisfiable；合法范围 → 206 Partial Content 分块格式：十六进制长度\\\\r\\\\n数据块\\\\r\\\\n，以 0\\\\r\\\\n\\\\r\\\\n 终止 范围请求服务器需先声明 ..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://interview.leeguoo.com/images/s_poetries_work_gitee_2019_12_18.webp"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-06-26T01:03:23.000Z"}],["meta",{"property":"article:modified_time","content":"2026-06-26T01:03:23.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"\\",\\"image\\":[\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2019_12_18.webp\\",\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2019_12_19.webp\\",\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2019_12_101.webp\\"],\\"dateModified\\":\\"2026-06-26T01:03:23.000Z\\",\\"author\\":[]}"]]},"headers":[{"level":2,"title":"简版速记","slug":"简版速记","link":"#简版速记","children":[]},{"level":2,"title":"数据压缩","slug":"数据压缩","link":"#数据压缩","children":[]},{"level":2,"title":"分块传输","slug":"分块传输","link":"#分块传输","children":[]},{"level":2,"title":"范围请求","slug":"范围请求","link":"#范围请求","children":[]},{"level":2,"title":"多段数据","slug":"多段数据","link":"#多段数据","children":[]},{"level":2,"title":"小结","slug":"小结","link":"#小结","children":[]}],"git":{"updatedTime":1782435803000,"contributors":[{"name":"leeguooooo","email":"guoli@zhihu.com","commits":3},{"name":"guoli","email":"guoli@zhihu.com","commits":1}]},"autoDesc":true,"filePathRelative":"http 基础-13-HTTP传输大文件.md","excerpt":"<h2>简版速记</h2>\\n<table>\\n<thead>\\n<tr>\\n<th>手段</th>\\n<th>关键请求头 / 响应头</th>\\n<th>状态码</th>\\n<th>典型场景</th>\\n</tr>\\n</thead>\\n<tbody>\\n<tr>\\n<td>数据压缩</td>\\n<td><code>Accept-Encoding</code> / <code>Content-Encoding: gzip|br</code></td>\\n<td>200</td>\\n<td>HTML/CSS/JS 等文本</td>\\n</tr>\\n<tr>\\n<td>分块传输</td>\\n<td><code>Transfer-Encoding: chunked</code></td>\\n<td>200</td>\\n<td>流式/动态内容，body 长度未知</td>\\n</tr>\\n<tr>\\n<td>范围请求（单段）</td>\\n<td><code>Range: bytes=x-y</code> / <code>Content-Range: bytes x-y/total</code></td>\\n<td>206</td>\\n<td>视频拖拽、断点续传</td>\\n</tr>\\n<tr>\\n<td>范围请求（多段）</td>\\n<td><code>Range: bytes=x1-y1,x2-y2</code> / <code>Content-Type: multipart/byteranges; boundary=xxx</code></td>\\n<td>206</td>\\n<td>一次请求多个不连续片段</td>\\n</tr>\\n</tbody>\\n</table>"}');export{_ as comp,u as data};
