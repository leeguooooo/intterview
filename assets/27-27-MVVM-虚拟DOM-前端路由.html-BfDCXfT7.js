import{_ as e,c as s,o as n,a}from"./app-C51XJ4NN.js";const o="/images/s_poetries_work_gitee_2020_07_fe_27.webp",c="/images/s_poetries_work_gitee_2020_07_fe_28.webp",t="/images/s_poetries_work_gitee_2020_07_fe_29.webp",l={},p=a('<h1 id="_27-mvvm-虚拟dom-前端路由" tabindex="-1"><a class="header-anchor" href="#_27-mvvm-虚拟dom-前端路由"><span>27 MVVM/虚拟DOM/前端路由</span></a></h1><h3 id="_27-1-mvvm" tabindex="-1"><a class="header-anchor" href="#_27-1-mvvm"><span>27.1 MVVM</span></a></h3><blockquote><p>涉及面试题：什么是 <code>MVVM</code>？比之 <code>MVC</code> 有什么区别？</p></blockquote><p><strong>首先先来说下 View 和 Model</strong></p><ul><li><code>View</code> 很简单，就是用户看到的视图</li><li><code>Model</code> 同样很简单，一般就是本地数据和数据库中的数据</li></ul><blockquote><p>基本上，我们写的产品就是通过接口从数据库中读取数据，然后将数据经过处理展现到用户看到的视图上。当然我们还可以从视图上读取用户的输入，然后又将用户的输入通过接口写入到数据库中。但是，如何将数据展示到视图上，然后又如何将用户的输入写入到数据中，不同的人就产生了不同的看法，从此出现了很多种架构设计。</p></blockquote><blockquote><p>传统的 <code>MVC</code> 架构通常是使用控制器更新模型，视图从模型中获取数据去渲染。当用户有输入时，会通过控制器去更新模型，并且通知视图进行更新</p></blockquote><p><img src="'+o+'" alt=""></p><ul><li>但是 <code>MVC</code> 有一个巨大的缺陷就是控制器承担的责任太大了，随着项目愈加复杂，控制器中的代码会越来越臃肿，导致出现不利于维护的情况。</li><li>在 <code>MVVM</code> 架构中，引入了 <code>ViewModel</code> 的概念。<code>ViewModel</code> 只关心数据和业务的处理，不关心 <code>View</code> 如何处理数据，在这种情况下，<code>View</code>和 <code>Model</code> 都可以独立出来，任何一方改变了也不一定需要改变另一方，并且可以将一些可复用的逻辑放在一个 <code>ViewModel</code> 中，让多个 <code>View</code> 复用这个 <code>ViewModel</code>。</li></ul><p><img src="'+c+'" alt=""></p><ul><li>以 <code>Vue</code> 框架来举例，<code>ViewModel</code> 就是组件的实例。<code>View</code> 就是模板，<code>Model</code> 的话在引入 <code>Vuex</code> 的情况下是完全可以和组件分离的。</li><li>除了以上三个部分，其实在 <code>MVVM</code> 中还引入了一个隐式的 <code>Binder</code> 层，实现了 <code>View</code> 和 <code>ViewModel</code> 的绑定</li></ul><p><img src="'+t+`" alt=""></p><ul><li>同样以 <code>Vue</code> 框架来举例，这个隐式的 <code>Binder</code> 层就是 <code>Vue</code> 通过解析模板中的插值和指令从而实现 <code>View</code> 与 <code>ViewModel</code> 的绑定。</li><li>对于 <code>MVVM</code>来说，其实最重要的并不是通过双向绑定或者其他的方式将 <code>View</code> 与 <code>ViewModel</code> 绑定起来，而是通过 <code>ViewModel</code> 将视图中的状态和用户的行为分离出一个抽象，这才是 <code>MVVM</code> 的精髓</li></ul><h3 id="_27-2-virtual-dom" tabindex="-1"><a class="header-anchor" href="#_27-2-virtual-dom"><span>27.2 Virtual DOM</span></a></h3><blockquote><p>涉及面试题：什么是 <code>Virtual DOM</code>？为什么 <code>Virtual DOM</code> 比原生 <code>DOM</code> 快？</p></blockquote><ul><li>大家都知道操作 <code>DOM</code> 是很慢的，为什么慢的原因以及在「浏览器渲染原理」章节中说过，这里就不再赘述了- 那么相较于 <code>DOM</code>来说，操作 <code>JS</code> 对象会快很多，并且我们也可以通过 <code>JS</code>来模拟 <code>DOM</code></li></ul><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="line">    <span class="token keyword">const</span> ul <span class="token operator">=</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token literal-property property">tag</span><span class="token operator">:</span> <span class="token string">&#39;ul&#39;</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token literal-property property">props</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token keyword">class</span><span class="token operator">:</span> <span class="token string">&#39;list&#39;</span></span>
<span class="line">      <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">      <span class="token literal-property property">children</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token literal-property property">tag</span><span class="token operator">:</span> <span class="token string">&#39;li&#39;</span><span class="token punctuation">,</span></span>
<span class="line">        <span class="token literal-property property">children</span><span class="token operator">:</span> <span class="token string">&#39;1&#39;</span></span>
<span class="line">      <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上述代码对应的 <code>DOM</code> 就是</p><div class="language-html line-numbers-mode" data-highlighter="prismjs" data-ext="html" data-title="html"><pre class="language-html"><code><span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>ul</span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&#39;</span>list<span class="token punctuation">&#39;</span></span><span class="token punctuation">&gt;</span></span></span>
<span class="line">      <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>li</span><span class="token punctuation">&gt;</span></span>1<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>li</span><span class="token punctuation">&gt;</span></span></span>
<span class="line">    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>ul</span><span class="token punctuation">&gt;</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>那么既然 <code>DOM</code> 可以通过 <code>JS</code> 对象来模拟，反之也可以通过 <code>JS</code> 对象来渲染出对应的 <code>DOM</code>。当然了，通过 <code>JS</code> 来模拟 <code>DOM</code> 并且渲染对应的 <code>DOM</code> 只是第一步，难点在于如何判断新旧两个 <code>JS</code> 对象的最小差异并且实现局部更新 <code>DOM</code></li></ul><blockquote><p>首先 <code>DOM</code> 是一个多叉树的结构，如果需要完整的对比两颗树的差异，那么需要的时间复杂度会是 <code>O(n ^ 3)</code>，这个复杂度肯定是不能接受的。于是 <code>React</code> 团队优化了算法，实现了 <code>O(n)</code> 的复杂度来对比差异。 实现 <code>O(n)</code> 复杂度的关键就是只对比同层的节点，而不是跨层对比，这也是考虑到在实际业务中很少会去跨层的移动 <code>DOM</code> 元素。 所以判断差异的算法就分为了两步</p></blockquote><ul><li>首先从上至下，从左往右遍历对象，也就是树的深度遍历，这一步中会给每个节点添加索引，便于最后渲染差异</li><li>一旦节点有子元素，就去判断子元素是否有不同</li></ul><blockquote><p>在第一步算法中我们需要判断新旧节点的 <code>tagName</code> 是否相同，如果不相同的话就代表节点被替换了。如果没有更改 <code>tagName</code> 的话，就需要判断是否有子元素，有的话就进行第二步算法。</p></blockquote><blockquote><p>在第二步算法中，我们需要判断原本的列表中是否有节点被移除，在新的列表中需要判断是否有新的节点加入，还需要判断节点是否有移动。</p></blockquote><p>举个例子来说，假设页面中只有一个列表，我们对列表中的元素进行了变更</p><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="line">    <span class="token comment">// 假设这里模拟一个 ul，其中包含了 5 个 li</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">]</span></span>
<span class="line">    <span class="token comment">// 这里替换上面的 li</span></span>
<span class="line">    <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token number">4</span><span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>从上述例子中，我们一眼就可以看出先前的 <code>ul</code> 中的第三个<code>li</code>被移除了，四五替换了位置。</p></blockquote><blockquote><p>那么在实际的算法中，我们如何去识别改动的是哪个节点呢？这就引入了 <code>key</code> 这个属性，想必大家在 <code>Vue</code> 或者 <code>React</code> 的列表中都用过这个属性。这个属性是用来给每一个节点打标志的，用于判断是否是同一个节点。</p></blockquote><ul><li>当然在判断以上差异的过程中，我们还需要判断节点的属性是否有变化等等。</li><li>当我们判断出以上的差异后，就可以把这些差异记录下来。当对比完两棵树以后，就可以通过差异去局部更新 <code>DOM</code>，实现性能的最优化。</li></ul><blockquote><p>当然了 <code>Virtual DOM</code> 提高性能是其中一个优势，其实最大的优势还是在于：</p></blockquote><ul><li>将<code>Virtual DOM</code>作为一个兼容层，让我们还能对接非 <code>Web</code> 端的系统，实现跨端开发。</li><li>同样的，通过 <code>Virtual DOM</code>我们可以渲染到其他的平台，比如实现 <code>SSR</code>、同构渲染等等。</li><li>实现组件的高度抽象化</li></ul><h3 id="_27-3-路由原理" tabindex="-1"><a class="header-anchor" href="#_27-3-路由原理"><span>27.3 路由原理</span></a></h3><blockquote><p>涉及面试题：前端路由原理？两种实现方式有什么区别？</p></blockquote><blockquote><p>前端路由实现起来其实很简单，本质就是监听 <code>URL</code> 的变化，然后匹配路由规则，显示相应的页面，并且无须刷新页面。目前前端使用的路由就只有两种实现方式</p></blockquote><ul><li><code>Hash</code> 模式</li><li><code>History</code> 模式</li></ul><p><strong>1. Hash 模式</strong></p><blockquote><p><code>www.test.com/#/</code> 就是 <code>Hash URL</code>，当 <code>#</code> 后面的哈希值发生变化时，可以通过 <code>hashchange</code> 事件来监听到 <code>URL</code> 的变化，从而进行跳转页面，并且无论哈希值如何变化，服务端接收到的 <code>URL</code> 请求永远是 <code>www.test.com</code></p></blockquote><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="line">    window<span class="token punctuation">.</span><span class="token function">addEventListener</span><span class="token punctuation">(</span><span class="token string">&#39;hashchange&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token comment">// ... 具体逻辑</span></span>
<span class="line">    <span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><code>Hash</code> 模式相对来说更简单，并且兼容性也更好</p></blockquote><p><strong>2. History 模式</strong></p><blockquote><p><code>History</code> 模式是 <code>HTML5</code> 新推出的功能，主要使用 <code>history.pushState</code> 和 <code>history.replaceState</code> 改变 <code>URL</code></p></blockquote><ul><li>通过 <code>History</code> 模式改变 <code>URL</code> 同样不会引起页面的刷新，只会更新浏览器的历史记录。</li></ul><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="line">    <span class="token comment">// 新增历史记录</span></span>
<span class="line">    history<span class="token punctuation">.</span><span class="token function">pushState</span><span class="token punctuation">(</span>stateObject<span class="token punctuation">,</span> title<span class="token punctuation">,</span> <span class="token constant">URL</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token comment">// 替换当前历史记录</span></span>
<span class="line">    history<span class="token punctuation">.</span><span class="token function">replaceState</span><span class="token punctuation">(</span>stateObject<span class="token punctuation">,</span> title<span class="token punctuation">,</span> <span class="token constant">URL</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>当用户做出浏览器动作时，比如点击后退按钮时会触发 <code>popState</code> 事件</p></blockquote><div class="language-javascript line-numbers-mode" data-highlighter="prismjs" data-ext="js" data-title="js"><pre class="language-javascript"><code><span class="line">    window<span class="token punctuation">.</span><span class="token function">addEventListener</span><span class="token punctuation">(</span><span class="token string">&#39;popstate&#39;</span><span class="token punctuation">,</span> <span class="token parameter">e</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token comment">// e.state 就是 pushState(stateObject) 中的 stateObject</span></span>
<span class="line">      console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>e<span class="token punctuation">.</span>state<span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>两种模式对比</strong></p><ul><li><code>Hash</code>模式只可以更改 <code>#</code> 后面的内容，<code>History</code> 模式可以通过 <code>API</code> 设置任意的同源 <code>URL</code></li><li><code>History</code> 模式可以通过 <code>API</code> 添加任意类型的数据到历史记录中，<code>Hash</code> 模式只能更改哈希值，也就是字符串</li><li><code>Hash</code> 模式无需后端配置，并且兼容性好。<code>History</code> 模式在用户手动输入地址或者刷新页面的时候会发起 <code>URL</code> 请求，后端需要配置 <code>index.html</code> 页面用于匹配不到静态资源的时候</li></ul><h3 id="_27-4-vue-和-react-之间的区别" tabindex="-1"><a class="header-anchor" href="#_27-4-vue-和-react-之间的区别"><span>27.4 Vue 和 React 之间的区别</span></a></h3><ul><li><code>Vue</code> 的表单可以使用 <code>v-model</code> 支持双向绑定，相比于 <code>React</code> 来说开发上更加方便，当然了 <code>v-model</code> 其实就是个语法糖，本质上和 <code>React</code> 写表单的方式没什么区别</li><li>改变数据方式不同，<code>Vue</code> 修改状态相比来说要简单许多，<code>React</code> 需要使用 <code>setState</code> 来改变状态，并且使用这个 <code>API</code> 也有一些坑点。并且 <code>Vue</code> 的底层使用了依赖追踪，页面更新渲染已经是最优的了，但是 <code>React</code> 还是需要用户手动去优化这方面的问题。</li><li><code>React 16</code>以后，有些钩子函数会执行多次，这是因为引入 <code>Fiber</code> 的原因</li><li><code>React</code> 需要使用 <code>JSX</code>，有一定的上手成本，并且需要一整套的工具链支持，但是完全可以通过 <code>JS</code> 来控制页面，更加的灵活。<code>Vue</code> 使用了模板语法，相比于 <code>JSX</code> 来说没有那么灵活，但是完全可以脱离工具链，通过直接编写 <code>render</code> 函数就能在浏览器中运行。</li><li>在生态上来说，两者其实没多大的差距，当然 <code>React</code>的用户是远远高于<code>Vue</code> 的</li></ul>`,49),i=[p];function d(u,r){return n(),s("div",null,i)}const m=e(l,[["render",d],["__file","27-27-MVVM-虚拟DOM-前端路由.html.vue"]]),v=JSON.parse('{"path":"/%E9%AB%98%E9%A2%91%E8%80%83%E7%82%B9/27-27-MVVM-%E8%99%9A%E6%8B%9FDOM-%E5%89%8D%E7%AB%AF%E8%B7%AF%E7%94%B1.html","title":"27 MVVM/虚拟DOM/前端路由","lang":"zh-CN","frontmatter":{"description":"27 MVVM/虚拟DOM/前端路由 27.1 MVVM 涉及面试题：什么是 MVVM？比之 MVC 有什么区别？ 首先先来说下 View 和 Model View 很简单，就是用户看到的视图 Model 同样很简单，一般就是本地数据和数据库中的数据 基本上，我们写的产品就是通过接口从数据库中读取数据，然后将数据经过处理展现到用户看到的视图上。当然我们...","head":[["meta",{"property":"og:url","content":"https://interview.leeguoo.com/%E9%AB%98%E9%A2%91%E8%80%83%E7%82%B9/27-27-MVVM-%E8%99%9A%E6%8B%9FDOM-%E5%89%8D%E7%AB%AF%E8%B7%AF%E7%94%B1.html"}],["meta",{"property":"og:site_name","content":"前端面试题集锦"}],["meta",{"property":"og:title","content":"27 MVVM/虚拟DOM/前端路由"}],["meta",{"property":"og:description","content":"27 MVVM/虚拟DOM/前端路由 27.1 MVVM 涉及面试题：什么是 MVVM？比之 MVC 有什么区别？ 首先先来说下 View 和 Model View 很简单，就是用户看到的视图 Model 同样很简单，一般就是本地数据和数据库中的数据 基本上，我们写的产品就是通过接口从数据库中读取数据，然后将数据经过处理展现到用户看到的视图上。当然我们..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://interview.leeguoo.com/images/s_poetries_work_gitee_2020_07_fe_27.webp"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-06-25T14:39:47.000Z"}],["meta",{"property":"article:modified_time","content":"2026-06-25T14:39:47.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"27 MVVM/虚拟DOM/前端路由\\",\\"image\\":[\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2020_07_fe_27.webp\\",\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2020_07_fe_28.webp\\",\\"https://interview.leeguoo.com/images/s_poetries_work_gitee_2020_07_fe_29.webp\\"],\\"dateModified\\":\\"2026-06-25T14:39:47.000Z\\",\\"author\\":[]}"]]},"headers":[{"level":3,"title":"27.1 MVVM","slug":"_27-1-mvvm","link":"#_27-1-mvvm","children":[]},{"level":3,"title":"27.2 Virtual DOM","slug":"_27-2-virtual-dom","link":"#_27-2-virtual-dom","children":[]},{"level":3,"title":"27.3 路由原理","slug":"_27-3-路由原理","link":"#_27-3-路由原理","children":[]},{"level":3,"title":"27.4 Vue 和 React 之间的区别","slug":"_27-4-vue-和-react-之间的区别","link":"#_27-4-vue-和-react-之间的区别","children":[]}],"git":{"updatedTime":1782398387000,"contributors":[{"name":"leeguooooo","email":"guoli@zhihu.com","commits":2}]},"autoDesc":true,"filePathRelative":"高频考点/27-27-MVVM-虚拟DOM-前端路由.md","excerpt":"\\n<h3>27.1 MVVM</h3>\\n<blockquote>\\n<p>涉及面试题：什么是 <code>MVVM</code>？比之 <code>MVC</code> 有什么区别？</p>\\n</blockquote>\\n<p><strong>首先先来说下 View 和 Model</strong></p>\\n<ul>\\n<li><code>View</code> 很简单，就是用户看到的视图</li>\\n<li><code>Model</code> 同样很简单，一般就是本地数据和数据库中的数据</li>\\n</ul>\\n<blockquote>\\n<p>基本上，我们写的产品就是通过接口从数据库中读取数据，然后将数据经过处理展现到用户看到的视图上。当然我们还可以从视图上读取用户的输入，然后又将用户的输入通过接口写入到数据库中。但是，如何将数据展示到视图上，然后又如何将用户的输入写入到数据中，不同的人就产生了不同的看法，从此出现了很多种架构设计。</p>\\n</blockquote>"}');export{m as comp,v as data};
